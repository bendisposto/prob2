package de.prob.ui.operationview;

import org.eclipse.core.commands.AbstractHandler;
import org.eclipse.core.commands.ExecutionEvent;
import org.eclipse.core.commands.ExecutionException;
import org.eclipse.core.commands.IHandler;

public class RestartAnimationHandler extends AbstractHandler implements IHandler {

	public Object execute(final ExecutionEvent event) throws ExecutionException {

		//TODO: Implement RestartAnimationHandler
//		Injector injector = ServletContextListener.INJECTOR;
//		AnimationSelector selector = injector.getInstance(AnimationSelector.class);
//		
//		selector.getCurrentHistory().restartAnimation();
//		System.out.println(selector.getCurrentHistory());
		return null;
	}
}
