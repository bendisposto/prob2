import de.prob.bmotion.IBMotionGroovyObserver;
import de.prob.ui.api.ITool;

// The variable bms is the entry point to the BMotion Studio API

bms.registerGroovyObserver(
	[
		update: { ITool tool ->
			// This method is called whenever the model change in state
		}
	] as IBMotionGroovyObserver
)