package de.prob.model.staticinfo;

public class Instantiator {

	// public State intantiate(final String name, final Map<String, Values>
	// values) {
	// if (values.get(name) != null) return new State(name,values.get(name))
	// else {
	// Value v = evaluate(name);
	//
	// }
	//
	// }
	//
	// public State instantiate(final Schema s, final Map<String, Values>
	// values) {
	// State result = instantiate(s.name, values);
	// for (Schema subschema : s.getChildren()) {
	// State substate = instantiate(subschema, values);
	// result.add(substate);
	// }
	// }

}
